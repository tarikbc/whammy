<?xml version="1.0" encoding="utf-8"?>
<!--
  Filter chip background — selected (DESIGN.md §7.11): same surface_hi
  fill + r_sm 12dp shell as bg_chip_filter, but the hairline becomes a
  1.5dp `star` stroke (design's "star 1.5dp stroke" selected treatment).
  The label color swap (text -> text_hi) happens at the use site
  (MainActivity), same pattern as the video/setlist row badge chips.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
  <solid android:color="@color/surface_hi" />
  <corners android:radius="@dimen/r_sm" />
  <stroke android:width="1.5dp" android:color="@color/star" />
</shape>
