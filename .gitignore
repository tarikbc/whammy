# Design-agent verification artifacts (headless screenshots / logs)
.gstack/

# Android build outputs (added as the build lands)
/build/
/out/
*.apk
*.aab
*.dex
*.ap_
gen/
# Signing keys — never commit
*.jks
*.keystore
keystore/

# Prebuilt jars dropped in for the manual build (none committed yet)
libs/

# OS / editor
.DS_Store
.idea/
*.iml
