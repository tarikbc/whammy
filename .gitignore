# Design-agent verification artifacts (headless screenshots / logs)
.gstack/

# Android build outputs (added as the build lands)
/build/
/out/
*.apk
*.aab
*.dex
*.ap_
gen/
# Signing keys — never commit
*.jks
*.keystore
keystore/

# Vendored jars + AAR res/manifest for the manual build (see vendor-libs.sh
# for the exact fetch/extract commands — libs/aar/<name>/{res,AndroidManifest.xml}
# is what build.sh compiles+links to generate real androidx.* R classes)
libs/

# OS / editor
.DS_Store
.idea/
*.iml

# Internal workflow docs / scratch — not for the public repo
docs/superpowers/
.superpowers/
